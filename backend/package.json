{
  "name": "cross-bookmark-backend",
  "version": "1.0.0",
  "description": "Backend for Cross Bookmark application",
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "start": "uvicorn app:app --host=0.0.0.0 --port=$PORT",
    "build": "pip install -r requirements.txt"
  }
}
{
  "name": "your-project-name",
  "version": "1.0.0",
  "description": "Your project description",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yourusername/your-repo.git"
  },
  "keywords": [
    "keyword1",
    "keyword2"
  ],
  "author": "Your Name",
  "license": "ISC",
  "engines": {
    "node": ">=16.0.0"
  }
}
{
  "name": "your-package-name",
  "version": "1.0.0",
  "description": "Your package description",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/username/repo.git"
  },
  "keywords": [
    "keyword1",
    "keyword2"
  ],
  "author": "Your Name",
  "license": "ISC",
  "engines": {
    "node": ">=16.0.0"
  }
}